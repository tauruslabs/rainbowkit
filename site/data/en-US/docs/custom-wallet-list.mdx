---
title: Custom Wallet List
description: Customizing the wallet list
---

# Custom Wallet List

## Customizing the wallet list

You can import individual wallets from `'@merkletrade/rainbowkit/wallets'` along with the `connectorsForWallets` function to build your own list of wallets with their necessary connectors. This way you have full control over which wallets to display, and in which order.

For example, you can choose to only show Rainbow along with generic fallback wallets.

```tsx
import { connectorsForWallets } from '@merkletrade/rainbowkit';
import {
  rainbowWallet,
  walletConnectWallet,
} from '@merkletrade/rainbowkit/wallets';

const connectors = connectorsForWallets(
  [
    {
      groupName: 'Recommended',
      wallets: [rainbowWallet, walletConnectWallet],
    },
  ],
  {
    appName: 'My RainbowKit App',
    projectId: 'YOUR_PROJECT_ID',
  }
);
```

You can then pass your connectors to Wagmi's `createConfig`.

```tsx
import { connectorsForWallets } from '@merkletrade/rainbowkit';
import { createConfig } from 'wagmi';

const connectors = connectorsForWallets(/* ... */);

const config = createConfig({
  connectors,
  {/* Wagmi config */}
});

const queryClient = new QueryClient();

const App = () => (
  <WagmiProvider config={config}>
    <QueryClientProvider client={queryClient}>
      <RainbowKitProvider {...etc}>
        {/* Your App */}
      </RainbowKitProvider>
    </QueryClientProvider>
  </WagmiProvider>
);
```

### Built-in wallets

The following wallets are provided via the `wallet` object.

#### Generic Wallets

The following wallets are scoped to generic connection methods rather than specific apps. As a result, it’s recommended that these wallets are always included.

##### WalletConnect

This is a fallback wallet option designed for WalletConnect-based wallets that haven’t been provided by another wallet in the list.

**It's recommended that you always include this wallet in the list to ensure all WalletConnect-based wallets are supported.**

```tsx
import { walletConnectWallet } from '@merkletrade/rainbowkit/wallets';
```

##### Injected Wallet

This is a fallback wallet option designed for scenarios where `window.ethereum` exists but hasn’t been provided by another wallet in the list. This wallet will automatically hide itself from the list when the fallback is not necessary or if there is no injected wallet available.

**It's recommended that you always include this wallet in the list to ensure all injected wallets are supported.**

```tsx
import { injectedWallet } from '@merkletrade/rainbowkit/wallets';
```

##### Safe Wallet

For dApps that support Safe Apps to allow users to easily connect with their Safe vault or multi-sig, include the `safeWallet` option for automatic connections. The Safe option will only appear in the Safe Wallet browser environment.

```tsx
import { safeWallet } from '@merkletrade/rainbowkit/wallets';
```

### Specific Wallets

The following wallets are provided via the `wallet` object (in alphabetical order).

#### Argent

```tsx
import { argentWallet } from '@merkletrade/rainbowkit/wallets';
```

#### Bitget

```tsx
import { bitgetWallet } from '@merkletrade/rainbowkit/wallets';
```

#### Bifrost Wallet

```tsx
import { bifrostWallet } from '@merkletrade/rainbowkit/wallets';
```

#### Bitski

```tsx
import { bitskiWallet } from '@merkletrade/rainbowkit/wallets';
```

#### Bitverse

```tsx
import { bitverseWallet } from '@merkletrade/rainbowkit/wallets';
```

#### Bloom Wallet

```tsx
import { bloomWallet } from '@merkletrade/rainbowkit/wallets';
```

#### Bybit Wallet

```tsx
import { bybitWallet } from '@merkletrade/rainbowkit/wallets';
```

#### Brave Wallet

```tsx
import { braveWallet } from '@merkletrade/rainbowkit/wallets';
```

#### Coinbase Wallet

```tsx
import { coinbaseWallet } from '@merkletrade/rainbowkit/wallets';
```

#### Compass Wallet

```tsx
import { compassWallet } from '@merkletrade/rainbowkit/compassWallet';
```

#### Coin98 Wallet

```tsx
import { coin98Wallet } from '@merkletrade/rainbowkit/wallets';
```

#### Core

```tsx
import { coreWallet } from '@merkletrade/rainbowkit/wallets';
```

#### Dawn Wallet

```tsx
import { dawnWallet } from '@merkletrade/rainbowkit/wallets';
```

#### Enkrypt Wallet

```tsx
import { enkryptWallet } from '@merkletrade/rainbowkit/wallets';
```

#### FoxWallet

```tsx
import { foxWallet } from '@merkletrade/rainbowkit/wallets';
```

#### Frame Wallet

```tsx
import { frameWallet } from '@merkletrade/rainbowkit/wallets';
```

#### Frontier Wallet

```tsx
import { frontierWallet } from '@merkletrade/rainbowkit/wallets';
```

#### Gate Wallet

```tsx
import { gateWallet } from '@merkletrade/rainbowkit/wallets';
```

#### Ledger Live

```tsx
import { ledgerWallet } from '@merkletrade/rainbowkit/wallets';
```

#### imToken

```tsx
import { imTokenWallet } from '@merkletrade/rainbowkit/wallets';
```

#### Kraken Wallet

```tsx
import { krakenWallet } from '@merkletrade/rainbowkit/wallets';
```

#### Kresus Wallet

```tsx
import { kresusWallet } from '@merkletrade/rainbowkit/wallets';
```

#### Kaikas
 
```tsx 
import { kaikasWallet } from '@merkletrade/rainbowkit/wallets';
```

#### MetaMask

```tsx
import { metaMaskWallet } from '@merkletrade/rainbowkit/wallets';
```

#### MEW Wallet

```tsx
import { mewWallet } from '@merkletrade/rainbowkit/wallets';
```

#### OKX Wallet

```tsx
import { okxWallet } from '@merkletrade/rainbowkit/wallets';
```

#### Omni

```tsx
import { omniWallet } from '@merkletrade/rainbowkit/wallets';
```

#### 1inch Wallet

```tsx
import { oneInchWallet } from '@merkletrade/rainbowkit/wallets';
```

#### OneKey

```tsx
import { oneKeyWallet } from '@merkletrade/rainbowkit/wallets';
```

#### Phantom

```tsx
import { phantomWallet } from '@merkletrade/rainbowkit/wallets';
```

#### Rabby Wallet

```tsx
import { rabbyWallet } from '@merkletrade/rainbowkit/wallets';
```

#### Rainbow

```tsx
import { rainbowWallet } from '@merkletrade/rainbowkit/wallets';
```

#### Ramper Wallet

```tsx
import { ramperWallet } from '@merkletrade/rainbowkit/wallets';
```

#### Ronin Wallet

```tsx
import { roninWallet } from '@merkletrade/rainbowkit/wallets';
```

#### Safeheron

```tsx
import { safeheronWallet } from '@merkletrade/rainbowkit/wallets';
```

#### Taho

```tsx
import { tahoWallet } from '@merkletrade/rainbowkit/wallets';
```

#### Talisman

```tsx
import { talismanWallet } from '@merkletrade/rainbowkit/wallets';
```

#### Tokenary

```tsx
import { tokenaryWallet } from '@merkletrade/rainbowkit/wallets';
```

#### TokenPocket

```tsx
import { tokenPocketWallet } from '@merkletrade/rainbowkit/wallets';
```

#### Trust Wallet

```tsx
import { trustWallet } from '@merkletrade/rainbowkit/wallets';
```

#### Uniswap Wallet

```tsx
import { uniswapWallet } from '@merkletrade/rainbowkit/wallets';
```

#### XDEFI Wallet

```tsx
import { xdefiWallet } from '@merkletrade/rainbowkit/wallets';
```

#### Zerion

```tsx
import { zerionWallet } from '@merkletrade/rainbowkit/wallets';
```

### Examples

#### Ordering

Here are a few examples of displaying different wallets, in different order.

Show MetaMask along with generic fallback wallets.

```tsx
import { connectorsForWallets } from '@merkletrade/rainbowkit';
import {
  metaMaskWallet,
  walletConnectWallet,
} from '@merkletrade/rainbowkit/wallets';

const connectors = connectorsForWallets(
  [
    {
      groupName: 'Recommended',
      wallets: [metaMaskWallet, walletConnectWallet],
    },
  ],
  { appName: 'RainbowKit App', projectId: 'YOUR_PROJECT_ID' },
);
```

Show Rainbow, MetaMask and Coinbase along with generic fallback wallets.

```tsx
import { connectorsForWallets } from '@merkletrade/rainbowkit';
import {
  rainbowWallet,
  metaMaskWallet,
  coinbaseWallet,
  walletConnectWallet,
} from '@merkletrade/rainbowkit/wallets';

const connectors = connectorsForWallets(
  [
    {
      groupName: 'Suggested',
      wallets: [
        rainbowWallet,
        metaMaskWallet,
        coinbaseWallet,
        walletConnectWallet,
      ],
    },
  ],
  { appName: 'RainbowKit App', projectId: 'YOUR_PROJECT_ID' },
);
```

> Reminder: The order of the `wallets` array defines the order in which wallets will be displayed in the UI.

#### Multiple groups

You can use the `groupName` key to name different wallet groups. This is useful if you want to communicate to your users which wallets you recommend, as well as other possible wallets.

Recommend Rainbow and MetaMask, but also offer Coinbase along with generic fallback wallets.

```tsx
import { connectorsForWallets } from '@merkletrade/rainbowkit';
import {
  rainbowWallet,
  metaMaskWallet,
  coinbaseWallet,
  walletConnectWallet,
} from '@merkletrade/rainbowkit/wallets';

const connectors = connectorsForWallets(
  [
    {
      groupName: 'Recommended',
      wallets: [rainbowWallet, metaMaskWallet],
    },
    {
      groupName: 'Others',
      wallets: [coinbaseWallet, walletConnectWallet],
    },
  ],
  { appName: 'RainbowKit App', projectId: 'YOUR_PROJECT_ID' },
);
```
